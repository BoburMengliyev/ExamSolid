namespace ExamSolid
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //SOLID prinsiplari:

            // 1. S – Single Responsibility Principle (SRP)
            // Har bir class faqat bitta vazifaga ega bo'lishi kerak.

            // 2. O – Open/Closed Principle (OCP)
            // Kod kengaytirishga ochiq, o'zgartirishga yopiq bo'lishi kerak.

            // 3. L – Liskov Substitution Principle (LSP)
            // Voris class ota class o'rnida ishlay olishi kerak.

            // 4. I – Interface Segregation Principle (ISP)
            // Katta interfeys o'rniga kichik interfeyslarni ishlatamiz.

            // 5. D – Dependency Inversion Principle (DIP)
            // Yuqori darajadagi classlar(modullar) past darajadagi classlar(modullar)ga bog'liq bo'lmasligi kerak.
        }
    }
}
